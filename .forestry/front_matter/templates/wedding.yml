---
label: Wedding
hide_body: true
display_field: title
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: date
  type: datetime
  label: Date
  description: ''
  config:
    required: false
    date_format: 
    time_format: 
    display_utc: false
- name: cover_image
  type: file
  config:
    maxSize: 64
  label: Cover image
- name: catoegories
  type: field_group
  config: {}
  fields:
  - name: parent_category
    type: select
    default: []
    config:
      required: false
      options:
      - Wedding
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Parent Category
  - name: category
    type: select
    default: []
    config:
      required: false
      options:
      - Amalfi Coast
      - Tuscany
      - Como Lake
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Category
  label: Catoegories
- name: story
  type: blocks
  label: Story
  template_types:
  - two-columns-layout
  config:
    min: 
    max: 
- name: image___text
  type: field_group_list
  fields:
  - name: image_left
    type: file
    config:
      maxSize: 64
    label: Image Left
  - name: text_right
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: false
      schema:
        format: markdown
    label: Text Right
  config:
    min: 
    max: 
    labelField: 
  label: Image + Text
- name: image_gallery
  type: field_group_list
  fields:
  - name: gallery
    type: image_gallery
    config: {}
    label: Gallery
  config:
    min: 
    max: 
    labelField: 
  label: Image Gallery
